{
  "name": "yansdeco-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts --local",
    "deploy": "wrangler deploy --minify src/index.ts",
    "db:push": "wrangler d1 execute yansdeco --file=../schema/schema.sql --remote",
    "db:local": "wrangler d1 execute yansdeco --file=../schema/schema.sql --local"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
